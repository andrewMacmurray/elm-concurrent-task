# Multi page example with Effect pattern

An example of using concurrent task in a typical elm single page application.

1. Install dependencies:

```
npm install
```

2. Run the example:

```
npm start
```
